# Backend Configuration
PORT=3000
NODE_ENV=development

# Database Configuration (for non-docker local dev)
DB_PATH=./database.sqlite

# Frontend Configuration (Vite)
VITE_API_URL=http://localhost:3000
